// // components/SpecialMovie.tsx
// import MovieSlider from "./MovieSlider";

// export default function SpecialMovieSlider({
//   special,
//   movies,
// }: {
//   special: string;
//   movies: Movie[];
// }) {
//   const filtered = movies.filter((m) => m.name.toLowerCase().includes(special));
//   if (!filtered.length) return null;
//   return <MovieSlider title={special.toUpperCase()} movies={filtered} />;
// }
